{
  "name": "element-inspector-extension",
  "version": "0.6.1",
  "private": true,
  "scripts": {
    "check": "node --check background.js && node --check inspector.js && node --check content.js",
    "test": "node --test tests/*.test.js"
  }
}
